/*
 * @commitlint/config-conventional sets the conventional rules for commit messages, such as prefixing the commit type (feat, fix, chore, etc.) followed by a descriptive message.
 */
module.exports = {
  extends: ["@commitlint/config-conventional"],
};
